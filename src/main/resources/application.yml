spring:
  profiles:
    active: dev

---

spring:
  config:
    activate:
      on-profile: dev
  r2dbc:
    url: r2dbc:postgresql://192.168.1.18:5433
    username: postgres
    password: password


---

spring:
  config:
    activate:
      on-profile: prod
  r2dbc:
    url: r2dbc:postgresql://prod-server:5432/amarj
    username: postgres
    password: admin

---
server:
  port: 8083
  error:
    include-message: always

---
spring:
  r2dbc:
    show-sql: true

logging:
  level:
    org.springframework.data.r2dbc: DEBUG
    io.r2dbc.postgresql: TRACE
---
spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/emp-insights-db

---
spring:
  data:
    redis:
      host: localhost
      port: 6379



